SELECT MIN(student_rating) as min_student_rating,
       MAX(student_rating) as max_student_rating,
       ROUND(AVG(student_rating),2) as avg_student_rating,
       SUM(student_rating) AS sum_student_rating FROM student_on_course;
